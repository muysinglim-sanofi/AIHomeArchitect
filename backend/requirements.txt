fastapi==0.115.0
uvicorn[standard]==0.30.6
openai>=1.51.0
supabase>=2.7.4
httpx>=0.27.0
python-multipart==0.0.12
python-dotenv==1.0.1
Pillow>=10.0.0
# KHQR rendering: turns ABA's exact `qr_string` into a scannable symbol.
# Pure Python, no C extension, deterministic output. See backend/pwa_qr.py.
segno>=1.6.1
# Wave 5.17a — Supabase JWT validation in backend/auth.py
PyJWT>=2.9.0
# Phase B — FCM push (HTTP v1 OAuth2 service-account token). Imported lazily in
# push_service.py so the backend still boots if this isn't installed yet.
# `requests` is required by google.auth.transport.requests.Request (the OAuth
# token refresh transport) — google-auth does NOT pull it in on its own.
google-auth>=2.30.0
requests>=2.32.0
