<?xml version="1.0" encoding="utf-8"?>
<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
  <background android:drawable="@color/ic_launcher_background"/>
  <!-- No <inset>: the safe-zone margin is already baked into
       app_icon_foreground.png (logo ~66% of the canvas), matching the validated
       preview. NOTE: re-running `dart run flutter_launcher_icons` re-adds the
       16% inset here — remove it again, or the logo shrinks to ~45%. -->
  <foreground android:drawable="@drawable/ic_launcher_foreground"/>
</adaptive-icon>
